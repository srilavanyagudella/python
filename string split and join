def split_and_join(line):
    # 1. Split the string into a list using the space " " as a delimiter
    words = line.split(" ")
    
    # 2. Join the list of words back together using a hyphen "-"
    result = "-".join(words)
    
    return result

if __name__ == '__main__':
    line = input()
    result = split_and_join(line)
    print(result)
